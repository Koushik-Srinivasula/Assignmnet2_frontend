version: 0.2

phases:
  build:
    commands:
      - echo "Cloning repository..."
      - git clone https://github.com/Koushik-Srinivasula/Assignmnet2_frontend
      - echo "Syncing files with S3 bucket..."
      - aws s3 sync . s3://photos-bucket-ks

artifacts:
  files:
    - '**/*'
